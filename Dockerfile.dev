# Development container for Vite + React
FROM node:20-alpine

WORKDIR /app

# Install deps (cached layer)
COPY package*.json ./
RUN npm ci

# Source will be bind-mounted via docker-compose.dev.yml
EXPOSE 5173

# Important: --host 0.0.0.0 makes Vite reachable outside the container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
